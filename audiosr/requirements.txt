# AudioSR Server Dependencies
# Versatile Audio Super-Resolution at Scale
#
# NOTE: audiosr==0.0.7 pins numpy<=1.23.5 which is incompatible with Python 3.12.
# We install audiosr with --no-deps and manage its dependencies here with relaxed pins.
# Install with: pip install setuptools wheel && pip install --no-deps audiosr==0.0.7 && pip install -r requirements.txt

# Build dependencies (Python 3.12+ removed distutils)
setuptools>=69.0
wheel>=0.42.0

# Web framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# HTTP client (for URL-based audio fetching)
httpx>=0.25.0

# Config
pyyaml>=6.0
pydantic>=2.0

# Audio processing
soundfile>=0.12.0
librosa>=0.10.0

# PyTorch stack
torch>=2.0.0
torchaudio>=2.0.0
torchvision>=0.15.0
torchcodec>=0.10.0

# AudioSR transitive dependencies (relaxed from audiosr==0.0.7 pins)
numpy>=1.24.0
scipy>=1.10.0
einops>=0.6.0
transformers>=4.30.0
huggingface-hub>=0.16.0
tqdm>=4.60.0
ftfy>=6.1.0
chardet>=5.0.0
pandas>=2.0.0
progressbar>=2.5
timm>=0.9.0
torchlibrosa>=0.1.0
unidecode>=1.3.0
phonemizer>=3.2.0
matplotlib>=3.7.0
