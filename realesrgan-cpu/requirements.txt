# Real-ESRGAN CPU Upscaler Server Dependencies
# CPU-only — no CUDA/GPU required
# NOTE: torch CPU must be installed separately with --index-url
# See install instructions in start.py

# Web framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# HTTP client (for URL-based file fetching from other services)
httpx>=0.25.0

# Config
pyyaml>=6.0
pydantic>=2.0

# Real-ESRGAN and dependencies
realesrgan>=0.3.0
basicsr>=1.4.2
facexlib>=0.3.0
gfpgan>=1.3.8

# Image/video processing
opencv-python-headless>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0

# FFmpeg wrapper (for video frame extraction/reassembly)
ffmpeg-python>=0.2.0
