# Canary-STT Server Dependencies
# NVIDIA Canary-Qwen-2.5B Speech-to-Text

# Web framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# HTTP client
httpx>=0.25.0

# Config
pyyaml>=6.0
pydantic>=2.0

# Audio processing
soundfile>=0.12.0
librosa>=0.10.0

# NeMo Toolkit (includes PyTorch, Transformers, etc.)
# Requires PyTorch 2.6+ for FSDP2 support
# SA3-M9: Pin to a release tag instead of HEAD to prevent supply-chain risk
nemo_toolkit[asr] @ git+https://github.com/NVIDIA/NeMo.git@v2.2.1
